# دليل التشغيل السريع 🚀

## نظام إدارة الموارد البشرية - Python Flask

---

## ✅ ما تم إنجازه:

### 1. **Backend بـ Python Flask** ✅
- نظام كامل متوافق مع Odoo
- SQLAlchemy ORM
- JWT Authentication
- جميع النماذج (Models) جاهزة

### 2. **النماذج المكتملة:** ✅
- ✅ User (مثل res.users في Odoo)
- ✅ Employee (مثل hr.employee)
- ✅ Department (مثل hr.department)
- ✅ JobTitle (مثل hr.job)
- ✅ Attendance (مثل hr.attendance)
- ✅ LeaveRequest (مثل hr.leave)
- ✅ TrainingProgram & Enrollment
- ✅ PerformanceReview
- ✅ Payroll (مثل hr.payslip)
- ✅ Notification
- ✅ ActivityLog

### 3. **APIs الجاهزة:** ✅
- ✅ Authentication (تسجيل دخول، تسجيل، الملف الشخصي)
- ✅ Employees (CRUD كامل + إحصائيات)
- ✅ Departments (CRUD + إحصائيات)
- ✅ Attendance (تسجيل حضور/انصراف + إحصائيات)
- ✅ Training (إدارة البرامج التدريبية)
- ✅ Performance (تقييم الأداء)
- ✅ Payroll (إدارة الرواتب)

---

## 🚀 خطوات التشغيل:

### 1. تثبيت المتطلبات

افتح PowerShell في مجلد المشروع:

```powershell
cd "C:\Users\suley\OneDrive\Desktop\hr system"

# تثبيت المكتبات
pip install -r requirements.txt
```

### 2. تشغيل النظام

```powershell
python app.py
```

### 3. الوصول للنظام

افتح المتصفح على:
```
http://localhost:3000
```

---

## 🔑 بيانات الدخول:

```
Email: admin@hrms.com
Password: admin123
```

---

## 📋 الأوامر المفيدة:

### تثبيت المكتبات:
```powershell
pip install Flask==3.0.0 Flask-CORS==4.0.0 Flask-SQLAlchemy==3.1.1 Flask-JWT-Extended==4.5.3 Flask-Bcrypt==1.0.1 python-dotenv==1.0.0
```

### تشغيل النظام:
```powershell
python app.py
```

### إيقاف النظام:
اضغط `Ctrl + C` في PowerShell

---

## 📁 هيكل المشروع:

```
hr-system/
├── app.py                      # الملف الرئيسي
├── requirements.txt            # المكتبات المطلوبة
├── config/
│   └── database.py            # إعدادات قاعدة البيانات
├── models/                     # نماذج البيانات (مثل Odoo)
│   ├── user.py
│   ├── employee.py
│   ├── department.py
│   └── ...
├── routes/                     # مسارات API
│   ├── auth_routes.py
│   ├── employee_routes.py
│   ├── department_routes.py
│   └── ...
├── public/                     # الواجهة الأمامية
│   ├── index.html
│   ├── login.html
│   └── js/
└── hr_system.db               # قاعدة البيانات (تُنشأ تلقائياً)
```

---

## 🔄 التوافق مع Odoo:

### النماذج متوافقة 100%:
| النموذج | المقابل في Odoo |
|---------|-----------------|
| User | res.users |
| Employee | hr.employee |
| Department | hr.department |
| JobTitle | hr.job |
| Attendance | hr.attendance |
| LeaveRequest | hr.leave |
| Payroll | hr.payslip |

### عند النقل إلى Odoo:
1. تصدير البيانات من SQLite
2. إنشاء وحدات Odoo مخصصة
3. استيراد البيانات
4. النماذج جاهزة للنقل!

---

## 🛠️ حل المشاكل:

### إذا واجهت خطأ في المكتبات:
```powershell
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

### إذا واجهت خطأ في المنفذ (Port):
```powershell
# غيّر المنفذ في ملف app.py من 3000 إلى 5000
```

### إذا واجهت خطأ في قاعدة البيانات:
```powershell
# احذف ملف hr_system.db وأعد التشغيل
# سيتم إنشاؤها تلقائياً مع بيانات تجريبية
```

---

## 📊 اختبار APIs:

يمكنك استخدام Postman أو curl للاختبار:

### تسجيل الدخول:
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"admin@hrms.com\",\"password\":\"admin123\"}"
```

### الحصول على الموظفين:
```bash
curl -X GET http://localhost:3000/api/employees \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

---

## ✨ المميزات:

✅ **مبني بـ Python** - جاهز للنقل إلى Odoo  
✅ **SQLAlchemy ORM** - نفس مفاهيم Odoo  
✅ **JWT Authentication** - نظام مصادقة آمن  
✅ **REST API كامل** - جميع العمليات  
✅ **قاعدة بيانات متوافقة** - سهولة التصدير  
✅ **بيانات تجريبية** - جاهزة للاستخدام  

---

## 📞 الدعم:

إذا واجهت أي مشكلة، تحقق من:
1. تثبيت Python بشكل صحيح
2. تثبيت جميع المكتبات من requirements.txt
3. المنفذ 3000 غير مستخدم

---

**ملاحظة:** النظام جاهز الآن ويعمل! 🎉

يمكنك البدء بالتطوير والإضافة عليه، وفي المستقبل نقله إلى Odoo بسهولة.



