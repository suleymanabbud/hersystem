# نظام إدارة الموارد البشرية 🏢
## مبني بـ Python Flask - متوافق مع Odoo

نظام متكامل لإدارة الموارد البشرية مبني بـ **Python + Flask** ومُصمم ليكون متوافقاً مع **Odoo** للانتقال السهل في المستقبل.

## 🌟 المميزات الرئيسية

- ✅ **مبني بـ Python Flask** - متوافق 100% مع Odoo
- ✅ **SQLAlchemy ORM** - نفس المفاهيم المستخدمة في Odoo
- ✅ **بنية نماذج متوافقة** - تحاكي نماذج Odoo (res.users, hr.employee, hr.department)
- ✅ **JWT Authentication** - نظام مصادقة آمن
- ✅ **REST API كامل** - جميع عمليات CRUD
- ✅ **واجهة عربية** - دعم كامل للغة العربية

## 📋 الوحدات المتوفرة

1. **إدارة المستخدمين والصلاحيات**
2. **إدارة الموظفين**
3. **الهيكل التنظيمي والإدارات**
4. **المسميات الوظيفية**
5. **نظام الحضور والانصراف**
6. **إدارة الإجازات**
7. **التدريب والتطوير**
8. **تقييم الأداء**
9. **نظام الرواتب**
10. **الإشعارات والتنبيهات**
11. **سجل النشاطات**

## 🛠️ التقنيات المستخدمة

```
Backend:
- Python 3.8+
- Flask 3.0
- SQLAlchemy (ORM)
- Flask-JWT-Extended
- Flask-Bcrypt
- SQLite (يمكن التبديل إلى PostgreSQL/MySQL)

Frontend:
- HTML5 + CSS3
- Tailwind CSS
- JavaScript (Vanilla)
- Font Awesome Icons
```

## 🚀 التثبيت والتشغيل

### 1. متطلبات النظام

```bash
Python 3.8 or higher
pip (Python package manager)
```

### 2. تثبيت المكتبات

```bash
# إنشاء بيئة افتراضية (اختياري لكن موصى به)
python -m venv venv

# تفعيل البيئة الافتراضية
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# تثبيت المتطلبات
pip install -r requirements.txt
```

### 3. إعداد قاعدة البيانات

```bash
# قاعدة البيانات ستُنشأ تلقائياً عند أول تشغيل
# تحتوي على بيانات تجريبية جاهزة
```

### 4. تشغيل السيرفر

```bash
python app.py
```

أو يمكن استخدام:

```bash
flask run --port=3000
```

### 5. الوصول للنظام

افتح المتصفح على:
```
http://localhost:3000
```

## 🔑 بيانات الدخول الافتراضية

```
Email: admin@hrms.com
Password: admin123
```

## 📁 هيكل المشروع

```
hr-system/
├── app.py                      # ملف التشغيل الرئيسي
├── requirements.txt            # المكتبات المطلوبة
├── README_PYTHON.md            # التوثيق
│
├── config/                     # الإعدادات
│   └── database.py            # إعدادات قاعدة البيانات
│
├── models/                     # نماذج قاعدة البيانات (مثل Odoo)
│   ├── __init__.py
│   ├── user.py                # res.users في Odoo
│   ├── employee.py            # hr.employee في Odoo
│   ├── department.py          # hr.department في Odoo
│   ├── job_title.py           # hr.job في Odoo
│   ├── attendance.py          # hr.attendance في Odoo
│   ├── leave.py               # hr.leave في Odoo
│   ├── training.py            # التدريب
│   ├── performance.py         # تقييم الأداء
│   ├── payroll.py             # الرواتب
│   ├── notification.py        # الإشعارات
│   └── activity_log.py        # سجل النشاطات
│
├── controllers/                # Controllers (مثل Odoo)
│   ├── __init__.py
│   ├── auth_controller.py
│   ├── employee_controller.py
│   └── ...
│
├── routes/                     # مسارات API
│   ├── __init__.py
│   └── api.py
│
├── middleware/                 # Middleware
│   └── auth.py
│
├── utils/                      # دوال مساعدة
│   └── helpers.py
│
├── public/                     # الواجهة الأمامية
│   ├── index.html             # الصفحة الرئيسية
│   ├── login.html             # صفحة تسجيل الدخول
│   ├── js/
│   │   ├── app.js
│   │   └── login.js
│   └── css/
│       └── style.css
│
└── hr_system.db               # قاعدة البيانات (SQLite)
```

## 🔄 التوافق مع Odoo

تم تصميم النظام ليكون متوافقاً مع Odoo:

### مقارنة النماذج

| النموذج في النظام | المقابل في Odoo |
|-------------------|------------------|
| `User` | `res.users` |
| `Employee` | `hr.employee` |
| `Department` | `hr.department` |
| `JobTitle` | `hr.job` |
| `Attendance` | `hr.attendance` |
| `LeaveRequest` | `hr.leave` |
| `Payroll` | `hr.payslip` |

### خطوات الانتقال إلى Odoo:

1. **تصدير البيانات** من قاعدة البيانات الحالية
2. **إنشاء وحدات Odoo** مخصصة
3. **استيراد البيانات** إلى Odoo
4. **تخصيص Views** حسب الحاجة

## 📚 API Documentation

### Authentication

```http
POST /api/auth/login
Content-Type: application/json

{
  "email": "admin@hrms.com",
  "password": "admin123"
}
```

```http
GET /api/auth/me
Authorization: Bearer {token}
```

### Employees

```http
GET /api/employees                 # قائمة الموظفين
GET /api/employees/{id}            # موظف محدد
POST /api/employees                # إضافة موظف
PUT /api/employees/{id}            # تحديث موظف
DELETE /api/employees/{id}         # حذف موظف
GET /api/employees/stats           # إحصائيات
```

### Departments

```http
GET /api/departments               # قائمة الإدارات
GET /api/departments/{id}          # إدارة محددة
POST /api/departments              # إضافة إدارة
PUT /api/departments/{id}          # تحديث إدارة
DELETE /api/departments/{id}       # حذف إدارة
```

### Attendance

```http
POST /api/attendance/check-in      # تسجيل حضور
POST /api/attendance/check-out     # تسجيل انصراف
GET /api/attendance                # سجلات الحضور
GET /api/attendance/stats          # إحصائيات الحضور
```

### Training

```http
GET /api/training                  # البرامج التدريبية
POST /api/training                 # إضافة برنامج
POST /api/training/{id}/enroll     # تسجيل موظف
```

### Performance

```http
GET /api/performance               # تقييمات الأداء
POST /api/performance              # إضافة تقييم
GET /api/performance/stats         # إحصائيات الأداء
```

### Payroll

```http
GET /api/payroll                   # سجلات الرواتب
POST /api/payroll                  # إضافة سجل راتب
POST /api/payroll/generate-monthly # إنشاء رواتب شهرية
PUT /api/payroll/{id}/approve      # تأكيد دفع
```

## 🔒 الأمان والصلاحيات

- **JWT Authentication** - مصادقة آمنة
- **Password Hashing** - تشفير كلمات المرور
- **Role-based Access** - صلاحيات حسب الأدوار
- **Activity Logging** - تسجيل جميع العمليات

### الأدوار المتاحة:

- `admin` - مدير النظام (صلاحيات كاملة)
- `hr` - موارد بشرية (إدارة الموظفين)
- `manager` - مدير (تقييم الأداء، الموافقات)
- `employee` - موظف (عرض البيانات الخاصة)

## 📊 قاعدة البيانات

حالياً يستخدم النظام **SQLite** للتطوير. للإنتاج يمكن التبديل إلى:

### PostgreSQL (موصى به للإنتاج)

```python
# في ملف .env
DATABASE_URL=postgresql://user:password@localhost/hr_system
```

### MySQL

```python
DATABASE_URL=mysql+pymysql://user:password@localhost/hr_system
```

## 🧪 الاختبار

```bash
# تشغيل الاختبارات (قريباً)
python -m pytest tests/
```

## 🚀 النشر (Deployment)

### استخدام Gunicorn

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:3000 app:app
```

### استخدام Docker

```dockerfile
# Dockerfile قريباً
```

## 🤝 المساهمة

نرحب بأي مساهمات لتحسين النظام!

## 📄 الترخيص

MIT License

## 📞 الدعم

للأسئلة والاستفسارات، يرجى فتح Issue على GitHub

---

**ملاحظة:** هذا النظام جاهز للاستخدام ويمكن نقله إلى Odoo بسهولة في المستقبل! 🎉



